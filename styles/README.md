# styles/

Globale Styles liegen in `app/globals.css` (Next.js App-Router-Konvention).

Dieser Ordner ist für zusätzliche, thematisch getrennte Stylesheets
reserviert (z. B. `print.css` oder komponentenübergreifende Utilities),
die bei Bedarf in `app/layout.tsx` importiert werden können.
