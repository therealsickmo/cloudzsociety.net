# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env*.local

# typescript
*.tsbuildinfo

# editor
.vscode/*
!.vscode/extensions.json
.idea

# runtime data written by the admin/dashboard (keep folder, ignore contents)
data/*.json
# …but keep the editable site content & settings in git so they sync
# across machines (dashboard runtime data stays ignored above).
!data/content.json
!data/settings.json
